FeeHunt website source currently lives in `../site`.

The BeSafe reference used a `website/` folder for marketing pages. FeeHunt
already had a production `site/` folder, so the integration keeps the existing
folder to avoid breaking Cloudflare Pages history and current assets.
